<div class="main" id="two">
  <div class="container">
    <div class="form">
      <%= form_for(@formality) do |f| %>
        <% if @formality.errors.any? %>
          <div id="error_explanation">
            <h2><%= pluralize(@formality.errors.count, "error") %> prohibited this formality from being saved:</h2>

            <ul>
            <% @formality.errors.full_messages.each do |message| %>
              <li><%= message %></li>
            <% end %>
            </ul>
          </div>
        <% end %>

        <div class="field">
          <%= f.label :name %><br>
          <%= f.text_field :name %>
        </div>
        <div class="field">
          <%= f.label :description %><br>
          <%= f.text_area :description %>
        </div>
        <div class="field">
          <%= f.label :image %><br>
          <%= f.text_field :image %>
        </div>
        <div class="actions">
          <%= f.submit %>
        </div>
      <% end %>
    </div>
  </div>
</div>