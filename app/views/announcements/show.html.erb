<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @announcement.title %>
</p>

<p>
  <strong>Description:</strong>
  <%= @announcement.description %>
</p>

<% if current_user %> <%= link_to 'Edit', edit_announcement_path(@announcement) %> <% end %> |
<%= link_to 'Back', announcements_path %>
